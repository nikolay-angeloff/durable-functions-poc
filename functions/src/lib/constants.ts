/** Must match Service Bus queue names in Bicep / parameters. */
export const QUEUE_AZURE = "form-azure";
export const QUEUE_M365 = "form-m365";

/** Notifications when a workflow step fails and human correction is needed. */
export const QUEUE_CORRECTION_NEEDED = "correction-needed";

export const CORRELATION_TABLE_NAME = "OrchestrationCorrelation";
